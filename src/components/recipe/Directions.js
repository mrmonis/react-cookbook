import React from 'react';

const Directions = ({directions}) => {
  return (
    <div className="directions">
      <h4>Directions</h4>
      <p>{directions}</p>
    </div>
  )
}

export default Directions;